# 草稿工作区

这里存放临时笔记、想法、写作提纲和待完善内容，供本地管理端与 Hermes 读取工作进度。

- 草稿不会进入 VitePress 前台、文章分类、标签、归档、RSS 或 Sitemap。
- 管理端按 `年/月/日` 创建草稿，并返回可复制的 `/docs/drafts/...md` 路径。
- `status` 可使用 `idea`、`writing`、`paused`、`done`。
- 正式发布时，请在管理端文章生成器中创建文章，再将草稿正文复制过去。
